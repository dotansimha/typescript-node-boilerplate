name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    - name: Install Dependencies
      run: yarn
    - name: Build
      run: yarn build
    - name: Test
      run: yarn test
